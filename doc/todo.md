1.写入持久记忆时，键值对的标识语义信息不够
-- 执行层问题，记忆被注入了，但是没有编译成为可执行策略，例如持久化记忆include_current_time=true并不能说明必须做什么、何时做、做到什么程度，
所以只停留在“参考信息”，不是“行为约束”。
todo:  把关键记忆转为程序规则

2.系统提示词中无法将持久记忆的键值对转化成可执行的约束
-- 表示层问题 从数据库取出的语义信息不够太弱，不能没有转化为可执行的约束
todo:把记忆从自由的key/value转化为schema

3.工具调用失败回答直接崩溃，输出错误栈
todo：优化回答提示